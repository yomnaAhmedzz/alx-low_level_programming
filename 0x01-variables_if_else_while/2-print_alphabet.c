#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	char n;

	for (n = 97; n <= 122; n++)
	{
		putchar (n);
	}
	putchar ('\n');

	return (0);
}
